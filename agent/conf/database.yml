connections:
  sqlite:
    driver: "sqlite3"
    dsn: "./db.sqlite"
  mysql:
    driver: "mysql"
    dsn: "${DB_USERNAME}:${DB_PASSWORD}@tcp(${DB_HOST}:${DB_PORT})/${DB_DATABASE}?charset=utf8&parseTime=true"
  inkstation_ecom:
    driver: "mysql"
    dsn: "${DB_USERNAME}:${DB_PASSWORD}@tcp(${DB_HOST}:${DB_PORT})/inkstation_ecom?charset=utf8&parseTime=true"
